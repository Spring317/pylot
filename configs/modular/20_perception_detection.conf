###############################################################################
# Module: Perception / Obstacle detection  (YOLOv8, your trained model)
# Swap detector by changing --obstacle_detector_model (yolo|efficientdet|
# faster-rcnn) or by replacing this whole file.
###############################################################################
--obstacle_detection
--obstacle_detector_model=yolo
--yolo_detection_model_path=dependencies/models/obstacle_detection/yolo/best.pt
--nosimulator_obstacle_detection
--obstacle_detection_min_score_threshold=0.3
###############################################################################
# Traffic lights: the TF traffic-light model is unavailable, so use the
# simulator's ground-truth traffic lights. Swap to --traffic_light_detection
# once a trained TL model is in place.
###############################################################################
--simulator_traffic_light_detection
###############################################################################
--visualize_detected_obstacles
