###############################################################################
# Module: Control  (PID - torch-free)
# Tracks the planned waypoints by actuating throttle/brake/steer.
# Swap --control to mpc (model-predictive) or simulator_auto_pilot.
###############################################################################
--control=pid
--stop_for_vehicles=True
--stop_for_people=True
--stop_for_traffic_lights=True
