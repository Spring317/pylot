###############################################################################
# Module: Planning  (Frenet Optimal Trajectory - C++, torch-free)
# Produces the waypoint trajectory the vehicle should follow, avoiding the
# predicted obstacles. Swap --planning_type to rrt_star / hybrid_astar /
# waypoint to use a different planner.
###############################################################################
--planning_type=waypoint
--target_speed=20
--obstacle_clearance_fot=1.75
--max_road_width_l=7.0
--visualize_waypoints
