###############################################################################
# Module: Prediction  (linear - torch-free)
# Extrapolates each tracked obstacle's future trajectory from its past motion.
# Swap --prediction_type to r2p2 for the neural predictor (requires torch ML).
###############################################################################
--prediction
--prediction_type=linear
--prediction_num_past_steps=10
--prediction_num_future_steps=10
--visualize_prediction
