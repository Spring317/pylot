###############################################################################
# Module: Perception / Obstacle tracking  (SORT - torch-free)
# SORT = Kalman filter + Hungarian matching (filterpy/numpy), no neural net.
# Swap --tracker_type to deep_sort / da_siam_rpn / center_track to use the
# neural trackers (those require the torch ML stack).
###############################################################################
--obstacle_tracking
--tracker_type=sort
--tracking_num_steps=10
--visualize_tracked_obstacles
